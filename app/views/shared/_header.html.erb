<% if user_signed_in? %>
  <div class="nav user">
    <%= t('.greeting') %><b class="nav-admin user-email"><%= current_user.first_name || current_user.email %></b>!

    <%= link_to t('.main_page'), root_path %> |
    <%= link_to t('.log_out'), destroy_user_session_path, method: :delete %>
  </div>
<% end %>
