<% flash.each do |key, message| %>
  <%= content_tag :p, sanitize(message, tags: %w(a p), attributes: %w(href target)), class: "alert alert-#{ApplicationHelper::FLASH_MESSAGE_STYLE[key.to_sym]}" %>
<% end %>
