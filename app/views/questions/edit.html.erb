<%= render 'form' %>

<p>
  <%= link_to 'Delete',
              question_path(@question),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
  <%= link_to 'Back', admin_test_path(@question.test) %>
</p>
