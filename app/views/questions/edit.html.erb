<%= render 'form' %>

<p>
  <%= link_to 'Delete',
              question_path(@question),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
  <%= link_to 'Back', question_path %>
</p>
