<div class="row justify-content-center mt-3">
  <div class="col col-md-6">
    <h2 class="text-center"><b><%= t('.header') %></b></h2>

    <div class="card border-primary mb-1">
      <div class="card-body">
        <h6>
          <b>
            <%= @question.body %>
          </b>
        </h6>
      </div>
    </div>

    <%= render 'form' %>

    <p><%= link_to t('links.back'), admin_question_path(@question), class: "btn btn-primary" %></p>
  </div>
</div>
